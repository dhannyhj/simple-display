# Vercel KV / Upstash Redis credentials
# Untuk local dev: kosongkan → app pakai in-memory store (data hilang saat restart)
# Untuk production: isi dari Vercel Dashboard > Storage > KV Store > .env.local
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
